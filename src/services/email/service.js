import crypton from "cryptonite";
import { randomString } from "helper";
import { Response } from "net-tools";

class EmailService {
  static sendVerification({email, code}){
    
  }

  static sendVerification({email, code}){
    
  }
}

// Exporting AuthService for external use
export default AuthService;
